name: currency_decimal_formatter
description: >-
  TextField formatter and utilities for converting text into 
  decimal currency amounts and vice-versa.
version: 0.0.5
homepage: https://github.com/yuriescl/flutter_currency_decimal_formatter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  decimal: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

